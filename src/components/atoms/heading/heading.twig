{% set tag = tag ?? 'h1' %}
{% set utility_classes =  utility_classes ?? [] %}

{% if tag %}
  <{{ tag }} {{ attributes.addClass(utility_classes|join(' ')) }}>{{ heading_content }}</{{ tag }}>
{% endif %}
