{#
/**
 * @file
 * Vartheme BS5 Starter Kit implementation for the 'input' type of checkbox form element.
 *
 * Available variables:
 * - attributes: HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

{% include "vartheme_bs5:input" with {
  type: 'checkbox',
  bs_form_control: false,
  bs_size: '',
  input_checkbox_utility_classes: ['form-check-input'],
  attributes: attributes
} %}
