import { Title, Subtitle, Description, Source, Meta, Primary, Stories, Controls } from '@storybook/addon-docs/blocks';
import * as comStories from './views-view-summary.stories.json';
import templateSource from './views-view-summary.twig?raw';

<Meta of={comStories} />

<Title />
<Subtitle />
<Description />

> ### Bootstrap documentation
> * [Typography — Lists](https://getbootstrap.com/docs/5.3/content/typography/#lists)

<Primary />

## Properties
<Controls />

<Stories />

## Notes
* Renders Drupal Views summary rows as a list.
* Used for glossary-style listings where items are grouped by first letter or category.
* `rows` contains the summary items rendered by Drupal Views.
* Supports `ul`/`ol` via the `type` property and Bootstrap utility classes.

## Source code
<Source code={templateSource} />
