import { Meta, Title, Subtitle, Description, Primary, Stories, Controls, Source } from '@storybook/addon-docs/blocks';
import * as comStories from './rich-text.stories.json';
import templateSource from './rich-text.twig?raw';

<Meta of={comStories} name="Docs"/>

<Title />
<Subtitle />
<Description />

<Primary />

## Properties
<Controls />

<Stories of={comStories} />

## Behavior
- `trim = false` always keeps original rich HTML.
- `strip_html` only works when `trim = true`.
- `trim = true` and `strip_html = true` trims plain text.
- `trim = true` and `strip_html = false` trims while keeping HTML output.

## Source code
<Source code={templateSource} />
