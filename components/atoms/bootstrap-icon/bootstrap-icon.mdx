import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Others/Bootstrap Icon" />

# Bootstrap Icon

A Bootstrap icon component using Bootstrap Icons CSS classes with rem-based sizing.

## Highlights

- Lightweight `<i>` output
- Icon size uses rem input
- Color uses Bootstrap class values directly
