{#
  * Container component.
  * Available config:
  * - attributes: Drupal attributes.
  * - withContainer: Boolean variable to add container class or not.
  * - content: The content block that will be inside Container component.
  * - container_utility_classes: An array of utility classes that can be used to add extra
  *                    Bootstrap utility classes or custom classes to this component.
  * - classes: The main array of classes for Container component.
#}

{% set container_utility_classes = container_utility_classes ? container_utility_classes : [] %}

{% set classes = []|merge(container_utility_classes) %}

{% if withContainer %}
  {% set classes = classes|merge(['container']) %}
{% endif %}

<div {{ attributes.addClass(classes) }}>
  {% block children %}
    {{ children }}
  {% endblock %}
</div>
