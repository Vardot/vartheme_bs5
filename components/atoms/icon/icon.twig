{#
  Icon component.

  Props:
  - icon (string): icon id from dropdown
#}

{% if icon %}
  {{ include('vartheme_bs5:bootstrap-icon', {
    icon: icon,
    icon_size: 'fs-4',
  }, with_context: false) }}
{% endif %}
