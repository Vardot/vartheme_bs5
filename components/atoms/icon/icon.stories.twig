{% stories Icon with {
  title: 'Base/Icon',
  parameters: {
    componentSubtitle: 'Bootstrap Icon component',
    docs: {
      description: {
        component: 'An icon component using Bootstrap Icons CSS classes.',
      },
      story: {
        inline: true,
      },
    },
  },
  argTypes: {
    icon: {
      name: 'Icon',
      control: 'select',
      options: ['people', 'facebook', 'twitter-x', 'instagram', 'linkedin', 'plus', 'check', 'info', 'search', 'person', 'arrow-right', 'arrow-left'],
    },
  },
} %}

{% story Default with {
  name: 'Default',
  args: {
    icon: 'people',
  },
} %}
{{ include('vartheme_bs5:icon') }}
{% endstory %}

{% story InfoIcon with {
  name: 'Info icon',
  args: {
    icon: 'info',
  },
} %}
{{ include('vartheme_bs5:icon') }}
{% endstory %}

{% story ArrowRight with {
  name: 'Arrow right',
  args: {
    icon: 'arrow-right',
  },
} %}
{{ include('vartheme_bs5:icon') }}
{% endstory %}

{% endstories %}
