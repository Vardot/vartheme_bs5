/**
 * @file
 * Behaviors for the VARTHEME_BS5_SUBTHEME theme.
 */

(function ($, _, Drupal) {
  Drupal.behaviors.VARTHEME_BS5_SUBTHEME = {
    attach: function () {
      // VARTHEME_BS5_SUBTHEME JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal);
